# Tool: `scripts/ollama_manifest_fix.py`

## Purpose

Repair Ollama on-disk manifests so current servers can list and load local models.

*Purpose source:* `module_docstring`

## Location

- Repository path: `scripts/ollama_manifest_fix.py`
- Run from repo root: `python scripts/ollama_manifest_fix.py`

## CLI

- **Has** ``if __name__ == '__main__'`` guard: **yes**
- Notable entry-related functions: `main`

### Parser text
- Fix Ollama local manifest layout (discoverability + digest/size consistency).

### Arguments (statically extracted)
- `--models`: Models directory (defaults to OLLAMA_MODELS or ~/.ollama/models).
- `manifest_paths`: Optional specific manifest files to fix; default: all manifests under manifests/*/*/*/*.
- `--dry-run`, action=`store_true`: Print actions without writing.
- `--no-repair-json`, action=`store_true`: Only mirror registry aliases; do not rewrite manifest JSON.
- `--no-mirror`, action=`store_true`: Do not write duplicate manifests under the canonical registry.ollama.ai host.

## Symbols (public)

- **def** `manifest_paths_under`
  - Match Ollama's manifest.Manifests glob: manifests/*/*/*/* (files only).
- **def** `parse_manifest_rel_path`
- **def** `canonicalize_digest`
- **def** `blob_path_for_digest`
- **def** `fix_manifest_obj`
  - Normalize config + layers digests and sizes against files under models_root/blobs. Returns (new_manifest_dict, warnings).
- **def** `go_style_json_lines`
  - Match encoding/json.Encoder.Encode: one JSON object + trailing newline.
- **def** `process_manifest_file`
  - Returns a report dict for one file.
- **def** `run_fix`
- **def** `build_arg_parser`
- **def** `main`

---

*Generated by `src/skills/tool_discovery.py` from `scripts/ollama_manifest_fix.py`.*