# Tool: `scripts/face_swap_batch.py`

## Purpose

Batch face swap processor for model photography.

*Purpose source:* `module_docstring`

## Location

- Repository path: `scripts/face_swap_batch.py`
- Run from repo root: `python scripts/face_swap_batch.py`

## CLI

- **Has** ``if __name__ == '__main__'`` guard: **yes**
- Notable entry-related functions: `main`

### Parser text
- Batch face swap processor

### Arguments (statically extracted)
- `--config`: Path to face_swap_batch.yaml
- `--init-config`, action=`store_true`: Write default YAML config and exit
- `--self-test`, action=`store_true`: Run synthetic self-test and exit

## Symbols (public)

- **class** `FaceSwapError` extends RuntimeError
  - Pipeline-level error.
- **class** `MarkdownLogger`
  - Simple markdown logger that appends sections.
- **class** `FaceSwapConfig`
- **def** `detect_faces`
  - Detect face rectangles in an image.
- **def** `apply_inswapper`
  - Apply face swap on one rectangle. model_path is accepted for API compatibility, runtime uses initialized swapper.
- **def** `apply_gfpgan`
  - Enhance swapped image using GFPGAN when available.
- **def** `single_swap`
  - Swap all faces in one target image.
- **def** `batch_swap`
  - Batch face swap over all jpg/png images in target_dir.
- **def** `main`

## Notes

- Mix of class-based implementation and CLI-style entry functions.

---

*Generated by `src/skills/tool_discovery.py` from `scripts/face_swap_batch.py`.*