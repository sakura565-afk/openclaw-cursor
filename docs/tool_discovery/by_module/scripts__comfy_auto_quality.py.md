# Tool: `scripts/comfy_auto_quality.py`

## Purpose

Universal auto-quality processor for ComfyUI images.

*Purpose source:* `module_docstring`

## Location

- Repository path: `scripts/comfy_auto_quality.py`
- Run from repo root: `python scripts/comfy_auto_quality.py`

## CLI

- **Has** ``if __name__ == '__main__'`` guard: **yes**
- Notable entry-related functions: `main`, `parse_args`

### Parser text
- ComfyUI auto-quality processor (face restore + denoise + upscale).

### Arguments (statically extracted)
- `--input`, dest=`input_path`: Input image path.
- `--output`, dest=`output_dir`: Output directory.
- `--gpu`: Preferred Comfy host.
- `--comfy-url`, dest=`comfy_url`: Override ComfyUI API URL.
- `--retries`: HTTP retry attempts.
- `--retry-backoff-sec`: Retry backoff multiplier.
- `--upscale`: Preferred upscale factor.
- `--self-test`, action=`store_true`: Run API/object_info checks and local quality analysis.

## Symbols (public)

- **class** `PipelineError` extends RuntimeError
  - Pipeline-level exception with user-friendly text.
- **class** `Config`
- **class** `MarkdownLogger`
- **class** `ComfyClient`
- **class** `AutoQualityProcessor`
- **def** `parse_args`
- **def** `run_self_test`
- **def** `process_pil_image`
  - Programmatic API entry point for PIL inputs. Example: from PIL import Image from scripts.comfy_auto_quality import process_pil_image result = process_pil_image(Image.open("photo.jpg"), output_dir="out", gpu="Work-PC")
- **def** `main`

## Notes

- Mix of class-based implementation and CLI-style entry functions.

---

*Generated by `src/skills/tool_discovery.py` from `scripts/comfy_auto_quality.py`.*