# Tool: `scripts/comfy_video_pipeline.py`

## Purpose

Универсальный пайплайн генерации видео мебели через ComfyUI API:

*Purpose source:* `module_docstring`

## Location

- Repository path: `scripts/comfy_video_pipeline.py`
- Run from repo root: `python scripts/comfy_video_pipeline.py`

## CLI

- **Has** ``if __name__ == '__main__'`` guard: **yes**
- Notable entry-related functions: `main`, `parse_args`

### Parser text
- Генерация и интерполяция видео мебели через ComfyUI (SD + RIFE + SUPIR).

### Arguments (statically extracted)
- `--input_image`, required: Путь к входной фотографии мебели.
- `--effect`, required: Тип эффекта камеры: rotate | zoom | pan
- `--duration`: Длительность видео в секундах.
- `--fps`: Целевой FPS итогового видео.
- `--output_video`: Путь к финальному mp4 файлу.
- `--frames_dir`: Папка для промежуточных кадров.
- `--comfy_url`: URL ComfyUI API.
- `--checkpoint`: Base checkpoint для SD (по требованию: realisticVisionV60B1_v12.safetensors).
- `--rife_models_dir`: Путь к директории моделей RIFE.
- `--rife_model_name`: Явное имя RIFE модели (опционально).
- `--base_gen_fps`: FPS базовой SD генерации.
- `--width`: Ширина кадра.
- `--height`: Высота кадра.
- `--steps`: Количество SD шагов KSampler.
- `--cfg`: CFG scale для SD.
- `--denoise`: Сила denoise в img2img.
- `--seed`: Базовый seed.
- `--supir_scale`: Коэффициент SUPIR upscale.
- `--no_supir`, action=`store_true`: Отключить SUPIR upscale даже если ноды доступны.
- `--retries`: Количество retry для API и операций.
- `--retry_backoff_sec`: Базовая пауза между retry.

## Symbols (public)

- **class** `PipelineError` extends RuntimeError
  - Бизнес-ошибка пайплайна.
- **class** `PipelineConfig`
- **class** `MarkdownLogger`
  - Простой markdown-логгер в memory/comfy_video_log.md.
- **class** `ComfyClient`
  - Клиент для ComfyUI HTTP API с retry-логикой.
- **def** `retry`
  - Декоратор retry для локальных операций (не HTTP).
- **class** `ComfyVideoPipeline`
- **def** `parse_args`
- **def** `main`

## Notes

- Mix of class-based implementation and CLI-style entry functions.

---

*Generated by `src/skills/tool_discovery.py` from `scripts/comfy_video_pipeline.py`.*