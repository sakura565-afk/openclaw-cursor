# Tool: `scripts/video_thumbnail_generator.py`

## Purpose

Thumbnail generator for furniture videos.

*Purpose source:* `module_docstring`

## Location

- Repository path: `scripts/video_thumbnail_generator.py`
- Run from repo root: `python scripts/video_thumbnail_generator.py`

## CLI

- **Has** ``if __name__ == '__main__'`` guard: **yes**
- Notable entry-related functions: `main`, `parse_args`

### Parser text
- Generate furniture video thumbnails.

### Arguments (statically extracted)
- `--input-dir`: Directory containing mp4 files.
- `--output-dir`: Directory for thumbnail jpgs.
- `--brand-text`: Brand text overlay.
- `--use-minimax`, action=`store_true`: Enable optional MiniMax enhancement.
- `--frame-count`: Candidate frames per video.
- `--log-path`: Markdown batch log path.
- `--verbose`, action=`store_true`: Enable debug logging.

## Symbols (public)

- **def** `setup_logger`
  - Build logger for console output.
- **def** `extract_frames`
  - Extract evenly sampled JPG frames from video into temp dir.
- **def** `select_best_frame`
  - Select frame with highest center-biased Laplacian sharpness.
- **def** `enhance_thumbnail`
  - Enhance selected frame for thumbnail readability.
- **def** `apply_branding`
  - Overlay brand text in lower-left with a translucent rounded backdrop.
- **def** `generate_all_sizes`
  - Create thumbnails in requested resolutions.
- **def** `batch_process`
  - Process all mp4 files in directory and export thumbnails.
- **def** `parse_args`
  - Parse CLI arguments.
- **def** `main`
  - CLI entrypoint.

---

*Generated by `src/skills/tool_discovery.py` from `scripts/video_thumbnail_generator.py`.*