# Tool: `src/skills/tool_discovery.py`

## Purpose

Static discovery for CLI scripts and library modules under ``scripts/`` and ``src/``.

*Purpose source:* `module_docstring`

## Location

- Repository path: `src/skills/tool_discovery.py`
- Run from repo root: `python src/skills/tool_discovery.py`

## CLI

- **Has** ``if __name__ == '__main__'`` guard: **yes**
- Notable entry-related functions: `main`

### Parser text
- Discover tools under scripts/ and src/ and emit manifest + docs.

### Arguments (statically extracted)
- `--repo-root`: Repository root (default: inferred from this file or OPENCLAW_REPO_ROOT).
- `--out-dir`
- `--write`, action=`store_true`: Write manifest.json, INDEX.md, and by_module/*.md.
- `--check`, action=`store_true`: Exit with non-zero status if outputs are stale.
- `--json`, action=`store_true`: Print manifest JSON to stdout (no files).

## Symbols (public)

- **class** `ArgSpec`
- **class** `SubcommandSpec`
- **class** `CliProfile`
- **class** `SymbolDoc`
- **class** `ToolRecord`
- **def** `analyze_file`
- **def** `discover`
- **def** `write_outputs`
- **def** `manifests_equivalent`
  - Compare ignoring generated_at timestamp.
- **def** `check_up_to_date`
- **def** `build_arg_parser`
- **def** `main`

## Notes

- Mix of class-based implementation and CLI-style entry functions.

---

*Generated by `src/skills/tool_discovery.py` from `src/skills/tool_discovery.py`.*