# Tool: `scripts/ollama_batch.py`

## Purpose

Run Ollama prompts in parallel and save responses to JSON.

*Purpose source:* `argparse`

## Location

- Repository path: `scripts/ollama_batch.py`
- Run from repo root: `python scripts/ollama_batch.py`

## CLI

- **Has** ``if __name__ == '__main__'`` guard: **yes**
- Notable entry-related functions: `main`, `parse_args`

### Parser text
- Run Ollama prompts in parallel and save responses to JSON.

### Subcommands
- `run` — Run prompts from a text file or JSON array.

### Arguments (statically extracted)
- `--file`, required: Input file with prompts.
- `--model`, required: Ollama model name.
- `--output`: Path to the JSON output file.
- `--parallel`: Maximum concurrent requests to send to Ollama.
- `--timeout`: Timeout in seconds for each Ollama request.
- `--retries`: Maximum retries after the initial failed attempt.
- `--backoff-base`: Base seconds for exponential backoff between retries.

## Symbols (public)

- **class** `PromptResult`
- **def** `positive_int`
- **def** `positive_float`
- **def** `parse_args`
- **def** `load_prompts`
- **def** `format_duration`
- **def** `print_progress`
- **def** `run_prompt`
- **def** `run_batch`
- **def** `build_output_payload`
- **def** `write_results`
- **def** `run_command`
- **def** `main`

## Notes

- Mix of class-based implementation and CLI-style entry functions.

---

*Generated by `src/skills/tool_discovery.py` from `scripts/ollama_batch.py`.*