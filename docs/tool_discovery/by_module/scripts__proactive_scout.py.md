# Tool: `scripts/proactive_scout.py`

## Purpose

Proactive scout for OpenClaw follow-up prediction.

*Purpose source:* `argparse`

## Location

- Repository path: `scripts/proactive_scout.py`
- Run from repo root: `python scripts/proactive_scout.py`

## CLI

- **Has** ``if __name__ == '__main__'`` guard: **yes**
- Notable entry-related functions: `main`

### Parser text
- Proactive scout for OpenClaw follow-up prediction.

### Subcommands
- `check` — Return a prepared follow-up if it is already cached.
- `status` — Show current scout cache and job state.
- `clear` — Clear scout cache and job records.
- `predict` — Predict follow-ups and optionally run them in the background.
- `_worker`

### Arguments (statically extracted)
- `question`: Question to look up in the speculative cache.
- `--scout-dir`: Override the scout state directory.
- `--scout-dir`: Override the scout state directory.
- `--scout-dir`: Override the scout state directory.
- `task_type`: Task type: image, code, video, or analysis.
- `result`: The assistant result that future follow-ups will build on.
- `--scout-dir`: Override the scout state directory.
- `--idle-seconds`: Only start background work when the system has been idle longer than this number.
- `--job-path`, required
- `--scout-dir`, required

## Symbols (public)

- **class** `Prediction`
- **def** `scout_predict`
- **def** `scout_check`
- **def** `scout_run_background`
- **def** `scout_status`
- **def** `scout_clear`
- **def** `build_parser`
- **def** `main`

## Notes

- Mix of class-based implementation and CLI-style entry functions.

---

*Generated by `src/skills/tool_discovery.py` from `scripts/proactive_scout.py`.*