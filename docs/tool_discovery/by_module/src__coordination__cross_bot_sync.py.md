# Tool: `src/coordination/cross_bot_sync.py`

## Purpose

Cross-bot coordination helpers for OpenClaw bots.

*Purpose source:* `module_docstring`

## Location

- Repository path: `src/coordination/cross_bot_sync.py`
- Run from repo root: `python src/coordination/cross_bot_sync.py`

## CLI

- **Has** ``if __name__ == '__main__'`` guard: **yes**
- Notable entry-related functions: `main`

### Parser text
- OpenClaw cross-bot sync coordinator

### Subcommands
- `sync` — Sync a MEMORY.md file with shared state.
- `handoff` — Claim, transfer, or release a task.
- `status` — Write bot status to the shared status file.
- `unlock` — Remove the shared lock file if it exists.

### Arguments (statically extracted)
- `--shared-dir`: Override the shared state directory (defaults to ~/.openclaw/shared).
- `--bot`, required: Bot name, for example 'main' or 'tasks'.
- `--memory-path`: Path to the local MEMORY.md file. Defaults to ./MEMORY.md.
- `task`: Task description to record in shared state.
- `--bot`, required: Bot performing the action.
- `--to`, dest=`target_bot`: Optional target bot for a handoff.
- `--release`, action=`store_true`: Release the task instead of claiming it.
- `--allow-takeover`, action=`store_true`: Allow taking over an existing task claim.
- `--bot`, required: Bot writing the status.
- `--state`, required: Short status string, such as 'idle'.
- `--task`: Optional current task description.
- `--details`: Optional JSON object with extra status metadata.

## Symbols (public)

- **def** `utc_now`
  - Return an ISO 8601 timestamp in UTC.
- **def** `normalize_task`
  - Normalize task descriptions so equivalent claims share a key.
- **def** `normalize_memory_key`
  - Normalize a memory line for de-duplication and conflict resolution.
- **def** `parse_memory_entries`
  - Convert markdown text into normalized memory entries.
- **def** `render_memory`
  - Render normalized memory entries back to a deterministic markdown file.
- **def** `read_json`
  - Read a JSON file and return a mapping, or a default on missing files.
- **def** `atomic_write_text`
  - Atomically write text content to a file.
- **def** `atomic_write_json`
  - Atomically write JSON content to a file.
- **class** `FallbackFileLock`
  - A small lock-file implementation using ``O_EXCL`` semantics.
- **class** `LockTimeoutError` extends TimeoutError
  - Raised when the shared lock cannot be acquired in time.
- **class** `FileLock`
  - Thin wrapper that normalizes lock handling across implementations.
- **def** `build_lock`
  - Create either an external ``filelock`` instance or the fallback lock.
- **class** `CrossBotSyncCoordinator`
  - Coordinator for shared bot state, memory sync, and status writes.
- **def** `json_dumps`
  - Render JSON for CLI output.
- **def** `build_parser`
  - Create the CLI parser.
- **def** `run_cli`
  - Run the CLI and print JSON responses.
- **def** `main`
  - Entrypoint for ``python -m src.coordination.cross_bot_sync``.

## Notes

- Mix of class-based implementation and CLI-style entry functions.

---

*Generated by `src/skills/tool_discovery.py` from `src/coordination/cross_bot_sync.py`.*