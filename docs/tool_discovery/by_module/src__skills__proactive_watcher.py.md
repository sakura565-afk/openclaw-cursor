# Tool: `src/skills/proactive_watcher.py`

## Purpose

Proactive skill watcher for OpenClaw.

*Purpose source:* `argparse`

## Location

- Repository path: `src/skills/proactive_watcher.py`
- Run from repo root: `python src/skills/proactive_watcher.py`

## CLI

- **Has** ``if __name__ == '__main__'`` guard: **yes**
- Notable entry-related functions: `main`

### Parser text
- Proactive skill watcher for OpenClaw.

*No* ``add_argument`` calls were found via static analysis. The script may build parsers dynamically or delegate to another module.

## Symbols (public)

- **class** `SkillRecord`
- **class** `ProactiveSkillWatcher`
- **def** `build_parser`
- **def** `main`

## Notes

- CLI entry guard present but no ``add_argument`` calls matched (dynamic argparse possible).
- Mix of class-based implementation and CLI-style entry functions.

---

*Generated by `src/skills/tool_discovery.py` from `src/skills/proactive_watcher.py`.*