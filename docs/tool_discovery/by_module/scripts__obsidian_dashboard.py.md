# Tool: `scripts/obsidian_dashboard.py`

## Purpose

Obsidian Vault Dashboard

*Purpose source:* `module_docstring`

## Location

- Repository path: `scripts/obsidian_dashboard.py`
- Run from repo root: `python scripts/obsidian_dashboard.py`

## CLI

- **Has** ``if __name__ == '__main__'`` guard: **yes**
- Notable entry-related functions: `main`, `parse_args`

### Parser text
- Obsidian Vault Dashboard

### Arguments (statically extracted)
- `--vault-path`: Path to Obsidian vault
- `--port`: Port for dashboard web server
- `--dump-json`, action=`store_true`: Scan vault and print JSON stats (without starting server)
- `--report-format`: Report format for --report output
- `--report`, action=`store_true`: Generate report and print to stdout (without starting server)

## Symbols (public)

- **def** `bytes_to_human`
  - Convert bytes to human-readable value.
- **def** `now_iso`
  - Current timestamp in ISO format.
- **def** `log_event`
  - Append markdown log entry to memory/obsidian_dashboard_log.md.
- **def** `normalize_note_name`
  - Normalize note path to Obsidian note key without extension.
- **def** `parse_wiki_target`
  - Parse wiki link target: [[path/to/note|alias]] -> path/to/note [[note#header]] -> note
- **def** `extract_wiki_links`
  - Extract raw wiki-link targets from markdown content.
- **def** `extract_tags`
  - Extract hashtags from content + tags from frontmatter.
- **def** `get_project_stats`
  - Return project statistics: { "project": "...", "file_count": int, "total_size": int, "total_size_human": str, "last_modified": "...", "types": {"md": 5, "png": 2} }
- **def** `collect_markdown_notes`
  - Build an index of markdown notes by possible wiki-link keys.
- **def** `find_broken_links`
  - Find broken internal wiki links for markdown files. Returns list of: {"source": "...", "target": "..."}
- **def** `scan_vault`
  - Scan Obsidian vault and return dashboard data dictionary.
- **def** `generate_report`
  - Generate dashboard report in markdown or html.
- **def** `create_app`
  - Create and configure Flask app.
- **def** `run_server`
  - Run Flask dashboard server.
- **def** `parse_args`
  - CLI args.
- **def** `main`

---

*Generated by `src/skills/tool_discovery.py` from `scripts/obsidian_dashboard.py`.*